[package]
name = "net-codec"
version = "0.1.0"
edition = "2021"

[dependencies]
shared-consensus = { path = "../../shared-rs/consensus" }
minicbor = { version = "0.25", features = ["std"] }
blake2b_simd = "1"
tracing = "0.1"

[dev-dependencies]
hex = "0.4"
